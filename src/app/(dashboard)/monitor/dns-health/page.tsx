import SubToolStub from "@/components/ui/SubToolStub";
export default function DnsHealthPage() {
  return <SubToolStub title="DNS Health" description="Multi-resolver comparison, DNSSEC validation, and propagation checks across global DNS infrastructure." />;
}
