import SubToolStub from "@/components/ui/SubToolStub";
export default function ProbePage() {
  return <SubToolStub title="Probe" description="Active host interrogation via direct packet exchange — port discovery, service fingerprinting, OS detection, and TLS inspection." />;
}
