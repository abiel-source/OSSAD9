import SubToolStub from "@/components/ui/SubToolStub";
export default function SubnetTablesPage() {
  return <SubToolStub title="Subnet & CIDR Tables" description="All prefix lengths /0–/32 with dotted-decimal mask, wildcard, total IPs, and usable host counts." />;
}
